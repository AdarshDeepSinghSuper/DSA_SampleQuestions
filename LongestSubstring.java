import java.util.ArrayList;
public class LongestSubstring {
    public static int longestSubstring(String S)
    {
        int x = 0;
        int n = S.length();
        ArrayList<Character>list = new ArrayList<>();
        int count =0;
        int max = 0;
        for(int i=0;i<n;i++)
        {
            char a = S.charAt(i)
        }

        // for(int i =0;i<n;i++)
        // {
        //     char a = S.charAt(i);
        //     list.add(a);
        //     int n = list.size();
        //     for(int j=0;j<n;j++)
        //     {
        //         if(ArrayList.get(j) == 'a')
        //         {

        //         }
        //     }

        // }
    //    - write a python program for file operation such that opeaning a file, reading from it , writing into it , closing it ,  deleating a file , and various operations 
    //     -wap to convert a integer to a roman numeral ;
    //     -wap that input a text file the program should all the of unique world a in a file in alphaberical order ;
         
        return x;
    }
}
